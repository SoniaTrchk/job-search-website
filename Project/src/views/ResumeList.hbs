<!DOCTYPE html>
<html>
<head>
    <title>Resumes</title>
    <meta charset="utf-8" />
</head>
<body>
<a href="/employer/my_page">Back to profile</a>
<table>

    {{#each resumes}}
        <tr>
            <td>{{this.position}}</td>
            <td>{{this.salary}}</td>
            <td>{{this.cv}}</td>
            <td>
                <form action="add_contract/{{this.resume_id}}" method="POST">
                    <input type="submit" value="Propose contract" />
                </form>
            </td>
        </tr>
    {{/each}}
</table>
</body>
<html>